#!/bin/sh -xe
git describe --tags > version.txt
if [ -d wheelhouse ]
then
    mv wheelhouse wheelhouse.old
fi
pip wheel --wheel-dir wheelhouse --find-links . --no-cache-dir --find-links wheelhouse.old -r requirements.txt
pip install --no-index --find-links wheelhouse -r requirements.txt
